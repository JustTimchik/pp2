fruits=["apple", "banana", "cherry", "date"]
sorted_fruits = sorted(fruits, key=lambda x: len(x)) #using the sorted function to sort the list fruits by the length of each fruit name
print(sorted_fruits)
