nums=[1, 2, 3, 4, 5]
squared = list(map(lambda x: x ** 2, nums)) #using the map function to apply a lambda function that squares each element in the list nums, and converting the result to a list
print(squared)
